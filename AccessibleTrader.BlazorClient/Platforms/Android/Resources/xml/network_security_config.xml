<?xml version="1.0" encoding="utf-8"?>
<!--
    Forbid cleartext (http://) traffic for all remote hosts. Loopback (localhost /
    127.0.0.1) remains cleartext-allowed so a locally-installed Ollama instance on
    device still works without HTTPS. Every exchange / analytics / LLM provider
    must go over TLS — this closes a trivial MITM surface on public Wi-Fi.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
